# Optional. Set this to enable real Web Push instead of local reminder timers.
# You also need a backend holding the matching VAPID private key — see README.
# Without it the app falls back to local scheduling and says so in Settings.
VITE_VAPID_PUBLIC_KEY=
